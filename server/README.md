# PolloRP Server

Simple WebSocket server for multiplayer (max 10 players).

## Run locally

```
cd server
npm install
npm start
```

The server listens on `ws://localhost:3001` by default.

## Environment

- `PORT` - port to listen on
- `MAX_PLAYERS` - max simultaneous players (default 10)
